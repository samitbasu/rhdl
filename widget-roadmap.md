# RHDL Widget Roadmap

A prioritized list of widgets to add to `crates/rhdl-fpga`. Priorities follow one rule: **reusability ≈ depth in the dependency graph**. Build the deepest dependencies first; the higher tiers compose them almost for free.

This document is the strategic counterpart to `CLAUDE.md`. CLAUDE.md says *how* to build a widget; this document says *which* to build *next* and *why*.

---

## Already in the tree

`core::{constant, counter, delay, dff, option, slice}`, `core::ram::{synchronous, asynchronous, option_sync, option_async, pipe_sync}`, `cdc::{synchronizer, cross_counter}`, `gray::{encode, decode}`, `fifo::{synchronous, asynchronous}` (with internal `read_logic`/`write_logic` and a testing harness), `pipe::{map, filter, filter_map, chunked}`, `stream::{map, filter, filter_map, flatten, zip, tee, chunked, stream_buffer, fifo_to_stream, stream_to_fifo, pipe_wrapper, xfer}`, `axi4lite::{basic, channel, core, register, stream}`, `tristate::simple`, `dsp::lerp`, `rng::xorshift`, `reset::{conditioner, negating_conditioner, negation}`, `lid::carloni`.

## Confirmed missing (verified by grep)

UART, SPI, I2C, PWM, edge detector, debouncer, arbiter, integer divider, CRC engine, FIR/MAC, priority encoder, barrel shifter, rotator, full AXI4, Wishbone, Ethernet MAC, PLL/MMCM wrapper.

---

## Tier 0 — Foundation primitives

These are tiny — most are 30–100 LOC kernels — and they appear inside almost every higher-level widget. Build first. Each one unblocks several downstream widgets.

| # | Widget | Why it's foundational |
|---|---|---|
| 1 | ~~Edge detector~~ (rising / falling / any) — shipped: `crates/rhdl-fpga/src/core/edge_detector.rs` | Used by every protocol PHY, every trigger circuit, every debouncer. The simplest possible RHDL kernel; perfect first widget for the LLM-assisted workflow. |
| 2 | ~~Pulse stretcher / one-shot~~ (parameterized cycle count) — shipped: `crates/rhdl-fpga/src/core/pulse_stretcher.rs` | Used by debouncer, watchdog, timeout logic, blink-on-event. Composes a counter with a held flag. |
| 3 | ~~N-stage synchronizer chain~~ — shipped: `crates/rhdl-fpga/src/cdc/synchronizer_chain.rs` | Generalizes the existing single-bit `Sync1Bit` to depth `N`. Required by every CDC pattern. |
| 4 | ~~Multi-bit handshake bridge~~ (slow CDC for any `T: Digital`) — shipped: `crates/rhdl-fpga/src/cdc/slow_crosser.rs`. 4-phase req/ack with single-bit synchronizers; data is held stable in W and sampled by R. | Currently absent — the only multi-bit CDC is the gray-code `cross_counter` inside async FIFO. Required for config buses, status registers, control crossing slow domains. |
| 5 | ~~Priority encoder~~ (binary index of lowest/highest set bit) — shipped: `crates/rhdl-fpga/src/core/priority_encoder.rs` | Required by arbiters, interrupt controllers, instruction decoders, leading-zero count. |
| 6 | ~~Decoder / one-hot ↔ binary converters~~ — shipped: `crates/rhdl-fpga/src/core/one_hot.rs` | Required by register-file address decode, demuxes, every state-machine indicator. |

## Tier 1 — Combinational utilities

| # | Widget | Required by |
|---|---|---|
| 7 | ~~Barrel shifter~~ (parameterized over data width and shift-amount width) — shipped: `crates/rhdl-fpga/src/core/barrel_shifter.rs`. Five modes (LSL/LSR/ASR/ROL/ROR) selected by `ShiftOp` enum. | Variable shifts, rotators, bit-field extraction, DSP scaling. |
| 8 | ~~Population count (popcount)~~ — shipped: `crates/rhdl-fpga/src/core/popcount.rs` | ECC, hash-table sizing, normalization, ML inference. |
| 9 | ~~Leading-zero count~~ — shipped: `crates/rhdl-fpga/src/core/leading_zeros.rs` | Floating/fixed-point normalization, priority logic. |
| 10 | ~~Wide carry-chain comparator~~ — shipped: `crates/rhdl-fpga/src/core/comparator.rs`. Unsigned, emits all five flags (`eq`/`lt`/`le`/`gt`/`ge`) at once. Signed variant deferred. |

## Tier 2 — Sequential building blocks

| # | Widget | Composes |
|---|---|---|
| 11 | ~~Debouncer~~ (parameterized sample period and signal type) — shipped: `crates/rhdl-fpga/src/core/debouncer.rs` | (1) edge detector + (2) pulse stretcher + counter. |
| 12 | ~~Round-robin arbiter~~ — shipped: `crates/rhdl-fpga/src/core/round_robin_arbiter.rs` | (5) priority encoder + a rotation register. Required by multi-master AXI, switch fabrics, DMA channels. |
| 13 | ~~Strict-priority arbiter~~ — shipped: `crates/rhdl-fpga/src/core/strict_priority_arbiter.rs` | Trivial variant of (12). |
| 14 | ~~Integer divider~~ (shift-subtract, **unsigned only**) — shipped: `crates/rhdl-fpga/src/core/divider.rs`. Signed-divide variant deferred. | The Rust `/` operator does not synthesize in `#[kernel]`; you must instantiate this for any divide. Required by baud-rate generation, fixed-point math. |
| 15 | ~~Multiply-accumulate (MAC) unit~~ — shipped: `crates/rhdl-fpga/src/core/mac.rs`. Single-cycle, unsigned, full-precision intermediate via `DynBits::xmul`. Signed variant deferred. | FIR/IIR filters, DSP pipelines, ML inference. |
| 16 | ~~CRC engine~~ (bit-serial; parameterizable polynomial, width, init) — shipped: `crates/rhdl-fpga/src/core/crc.rs`. Reflect / xor-out deferred — apply in software at message boundary, or add a wrapper widget. | UART, Ethernet MAC, SPI flash, USB, every packet validation. |
| 17 | ~~Generic memory-mapped register file~~ — shipped: `crates/rhdl-fpga/src/core/register_file.rs`. `N` registers of any `T: Digital + Default`, combinational read + registered write, bus-agnostic. | Every peripheral. The existing `axi4lite::register` is bus-coupled; this is a strict generalization that any bus adapter can wrap. |

## Tier 3 — First-class protocol PHYs

These are what users *want*. They become straightforward once Tiers 0–2 exist. Entries 23–24 are the high-end protocol PHYs — fully-featured, register-mapped, and composing several Tier 0–2 widgets to demonstrate the reusability dividend in practice.

| # | Widget |
|---|---|
| 18 | ~~UART TX~~ — shipped: `crates/rhdl-fpga/src/core/uart_tx.rs` (8-N-1, runtime divisor). ~~UART RX~~ — shipped: `crates/rhdl-fpga/src/core/uart_rx.rs` (8-N-1, mid-baud sampling). ~~Full-duplex UART with FIFOs~~ — shipped: `crates/rhdl-fpga/src/core/uart.rs`. Composes TX+RX+two `SyncFIFO`s. |
| 19 | ~~SPI master~~ — shipped: `crates/rhdl-fpga/src/core/spi_master.rs`. Mode 0, MSB-first, 4-wire (sclk/mosi/miso/cs_n), 2 FPGA cycles per SPI bit. Other modes / bit orders deferred. |
| 20 | ~~SPI slave~~ — shipped: `crates/rhdl-fpga/src/core/spi_slave.rs`. Mode 0, MSB-first, oversampled `sclk_in` via FPGA clock with edge detection. Bidirectional (samples MOSI, drives MISO from `tx_data` latched at CS assertion). |
| 21 | ~~I2C master~~ (write-only, single-byte v1) — shipped: `crates/rhdl-fpga/src/core/i2c_master.rs`. Open-drain `scl_drive_low` / `sda_drive_low` outputs (wrap with `tristate::simple` at the pad). Read transactions, clock stretching, multi-byte bursts, multi-master arbitration deferred. |
| 22 | ~~PWM generator~~ — shipped: `crates/rhdl-fpga/src/core/pwm.rs`. Saw-tooth counter + comparator, period = `2^N` cycles. |
| 23 | ~~Half-duplex / 3-wire SPI master~~ — shipped: `crates/rhdl-fpga/src/core/half_spi_master.rs`. Mode 0 / MSB-first / 2 FPGA cycles per SPI bit. Runtime-configurable `write_bits`, `read_bits`, `turnaround` per transaction. Exposes `(sdio_oe, sdio_out)` for the host to wrap with `tristate::simple` (3-wire) or use `sdio_out` as MOSI directly (4-wire). Original entry follows: write `N` bits then read `M` bits in one CS-asserted transaction with configurable turnaround. 3-wire mode shares `SDIO` via `tristate::simple`; 4-wire mode supports asymmetric devices that keep separate MOSI/MISO. State machine: `Idle → Write(N bits) → Turnaround → Read(M bits) → Done`. Composes (1) edge detector, (3) sync chain on `sdio_in`/`miso`, `core::counter`, `core::dff` shift registers, `tristate::simple`. The first widget that genuinely exercises the tristate category end-to-end. References: National Semiconductor *Microwire* (AN-452); Bosch BMP280 / BMI270 datasheets ("SPI 3-wire" sections); ST LIS3DH; TI ADS1015. |
| 24 | ~~Intel 16550A-compatible UART~~ (register-mapped v1, not bit-compatible) — shipped: `crates/rhdl-fpga/src/core/bus_uart.rs`. Wraps the shipped `core::uart` with a 4-register memory-mapped interface (DATA at 0x0, STATUS at 0x1, 0x2/0x3 reserved). 8N1 only, baud rate fixed at construction, single combined irq (asserted while RX FIFO non-empty). NOT bit-compatible with Linux `8250_core` or QEMU `hw/char/serial.c`; full register-bit compatibility tracked as v2 (DLL/DLM with DLAB, IIR with priority encoding, MSR/MCR/FCR, programmable LCR for word length/parity/stop bits, hardware handshake, loopback, break detect/generate). Original entry follows: full RS-232 with programmable baud rate (DLL/DLM divisor latch + 16× sample clock), word length (5/6/7/8), parity (none/even/odd/mark/space), stop bits (1/1.5/2), hardware handshake (RTS/CTS/DTR/DSR/DCD/RI), 16-byte TX/RX FIFOs with programmable trigger levels, modem-status interrupts via IIR, loopback mode, break detect/generate, line-status flags (overrun/parity/framing). Register-level compatible with Linux `8250_core`, QEMU `hw/char/serial.c`, and every PC-derived firmware stack — meaning a synthesized RHDL UART drops into a SoC and existing software talks to it without modification. Composes (18) bare UART TX/RX, (14) integer divider for the baud-rate generator, `fifo::synchronous` on each direction, **(17) generic memory-mapped register file (hard dependency — must ship before this)**, (5) priority encoder for the IIR, (1) edge detector + (3) sync chain on modem-status pads. The DLAB bank-switch in LCR is the canonical demo for the register-file widget's banked-register support. References: Intel *PC16550D Universal Asynchronous Receiver/Transmitter With FIFOs* datasheet; Linux `drivers/tty/serial/8250/8250_core.c`; QEMU `hw/char/serial.c`; *Linux Serial HOWTO* (Lawyer). |
| 25 | ~~CAN master~~ (Classical CAN 2.0A, **TX-only v1**) — shipped: `crates/rhdl-fpga/src/core/can_master.rs`. 11-bit standard ID + DLC 0..=8 data frame, frame walk SOF → ID → RTR/IDE/r0 → DLC → Data → CRC → CrcDelim → AckSlot → AckDelim → EOF → IFS, full 5-same-bit stuffing in the SOF-through-CRC region, CRC-15 polynomial `0x4599`. ACK slot driven recessive (no ACK detection in v1). No receiver, no acceptance filtering, no REC/TEC error counters, no bus-off state, no SJA1000 register interface, no programmable bit timing — all v2 follow-ups. Pairs with an external CAN transceiver (TJA1050 / MCP2551 / SN65HVD230). Original entry follows: bit-timing generator with programmable Sync / Prop / Phase1 / Phase2 segments and time-quantum prescaler; bit-stuffer and destuffer enforcing the 5-same-bit rule; 15-bit CRC with polynomial `0x4599`; frame-transmit FSM `SOF → arbitration → control → data → CRC → ACK → EOF` with lossless arbitration-loss recovery; frame-receive FSM with acceptance filters (ID + mask pairs); REC/TEC error counters with ISO 11898-1 increment/decrement rules; bus-off / error-passive / error-active state tracking. Pairs with an external CAN transceiver (TJA1050 / MCP2551 / SN65HVD230) — only the digital TX/RX lines connect to the FPGA. Register-mapped Philips/NXP **SJA1000-compatible** so Linux `drivers/net/can/sja1000/sja1000.c` and the OpenCores CAN tooling talk to it without modification. Composes (1) edge detector for bus-state changes, (3) sync chain on the RX line from the transceiver, (14) integer divider for the bit-time prescaler, (16) CRC engine specialized to CAN's 15-bit polynomial (a minor extension to the bit-serial CRC), `fifo::synchronous` for TX and RX message buffers, **(17) generic memory-mapped register file (hard dependency)**, (5) priority encoder for IRQ vector. The bus-state and frame-type variants are textbook RHDL enum-with-payload demos: `enum Frame { Data { id, dlc, payload }, Remote { id }, Error, Overload }` and `enum NodeState { ErrorActive, ErrorPassive, BusOff }`. References: ISO 11898-1 (Classical CAN) and ISO 11898-2 (high-speed PHY); *Bosch CAN Specification 2.0 Parts A & B*; Philips / NXP SJA1000 datasheet (the canonical "famous CAN" register interface, the CAN equivalent of the 16550A); Linux `drivers/net/can/sja1000/`; OpenCores CAN core by Igor Mohor (GPL); Microchip MCP2515 datasheet (alternative reference, SPI-attached). CAN-FD adds bit-rate switching, longer payloads (up to 64 bytes), and a different CRC — a natural follow-up widget once Classical CAN ships. |
| 26 | ~~WS2812 / NeoPixel driver~~ (single-pixel v1) — shipped: `crates/rhdl-fpga/src/core/ws2812.rs`. Runtime-configurable timings (`t0_high`, `t1_high`, `bit_period`, `latch_period`) cover WS2812B, WS2811, SK6812 RGB by changing constants. Multi-pixel host-managed (host strobes `send` per pixel, `latch` at end of frame). FIFO-streamed multi-pixel variant deferred. |
| 27 | ~~Dallas / Maxim 1-Wire master~~ (single-byte v1) — shipped: `crates/rhdl-fpga/src/core/one_wire_master.rs`. Three operations: `Reset` (with presence-pulse latch), `WriteByte` (8 bits LSB-first), `ReadByte` (8 bits LSB-first). Bus timings (8 fields: t_rst_low/sample/total, t_w0/w1, t_read_low/sample, t_slot) configurable via a `OneWireTimings` struct in *FPGA cycles*; same widget covers DS18B20 standard, overdrive, and short-line variants. Open-drain `(bus_oe, bus_out)` output for host wrap with `tristate::simple`. CRC-8 engine, ROM-search algorithm, overdrive autoswitch, parasitic-power strong-pullup, and slave widget all deferred to v2. Original entry follows: the canonical "1-Wire" trademarked protocol. Open-drain bidirectional bus where the master pulls low and slaves release the line for the master to sample. Slot timing: 60 µs slots with sub-slot read/write windows, 480 µs reset pulse, 60–240 µs presence-detect response. ROM commands (Search, Match, Skip, Read), function commands (slave-specific), 8-bit CRC with polynomial `0x31`. Drives DS18B20 temperature sensors, DS2401 silicon serial numbers, iButton products, DS2502 EPROMs. Composes `core::counter` for slot timing, **`tristate::simple`** (the second widget after half-duplex SPI to genuinely exercise it), (1) edge detector for presence-pulse detection, (16) CRC engine specialized to polynomial `0x31` — validates the Tier-2 CRC's polynomial flexibility — `fifo::synchronous` for the data-exchange buffer. ~250–400 LOC. Reference target chip: DS18B20 (a $2 part every electronics hobbyist has in a drawer). References: Maxim / Analog Devices Application Note 187 (1-Wire Communication Through Software); DS18B20 datasheet; Maxim 1-Wire ROM Search algorithm whitepaper; iButton family standards document. |
| 28 | ~~LIN bus master~~ (single-byte v1) — shipped: `crates/rhdl-fpga/src/core/lin_master.rs`. Composes `core::uart_tx`. Sends break + sync (0x55) + PID (with parity) + 1 data byte + classic checksum. Multi-byte data and enhanced checksum deferred. Original entry follows: Local Interconnect Network — single-wire automotive serial bus at 1–20 kbit/s, master-slave (exactly one master schedules all transactions; slaves respond when polled). Frame: 13-bit dominant break, sync byte `0x55`, 8-bit Protected ID (6 ID bits + 2 parity bits), 1–8 data bytes, classic or enhanced checksum. Used in every modern car for low-speed accessories — window/seat/mirror controllers, climate-control panels, rain sensors. Composes the existing UART TX/RX machinery from (18) almost wholesale; adds break generator/detector, sync-field timing, PID parity (combinational), classic and enhanced checksum (combinational fold over data bytes; enhanced also folds in the PID). Pairs with an external LIN transceiver (TJA1020 / MCP201 / ATA663211). ~200 LOC because most heavy lifting is already in the UART. Strategic value: paired with (25) CAN, this gives RHDL the full automotive low-speed bus stack — opens a market segment where Verilog dominates today. References: ISO 17987 series (LIN 2.2A); LIN Consortium Specification 2.2A; NXP TJA1020 datasheet; Microchip MCP201 datasheet; Atmel/Microchip ATA663211 datasheet. |
| 29 | ~~DHT22 / AM2302 humidity-temperature reader~~ — shipped: `crates/rhdl-fpga/src/core/dht22.rs`. Tunable timings cover DHT22 + DHT11. Output is the raw 40-bit frame; host masks for humidity/temperature/checksum (workaround for a DynBits→Bits<16> type-inference edge case). Original entry follows: popular hobbyist single-wire sensor protocol. Master pulls low for >18 ms to wake the sensor, then releases; sensor responds with an 80 µs low / 80 µs high acknowledge pulse, then transmits 40 bits (humidity high, humidity low, temperature high, temperature low, checksum) by varying the high-time of each bit-pulse: 26–28 µs = 0, 70 µs = 1. One transaction per second maximum. Composes `core::counter` for pulse-width measurement, (1) edge detector for level transitions, `tristate::simple` for the bidirectional data line, a small parity/checksum check. ~80 LOC — the smallest of the new entries. Useful as a beginner widget and as a building block for cheap environmental-sensing demos. References: Aosong DHT22 / AM2302 datasheet; Adafruit DHT library README; SparkFun DHT22 hookup guide. The sibling DHT11 has lower precision but identical wire protocol — the same widget covers both with a precision-parameter difference. |
| 30 | ~~IR remote receiver~~ (NEC protocol, receiver only) — shipped: `crates/rhdl-fpga/src/core/ir_nec_rx.rs`. Decodes the NEC 32-bit format from the pre-demodulated output of a 38 kHz IR receiver module (TSOP4838 / VS1838B). Edge-driven 6-state FSM with per-state tick counter; thresholds passed in via a `NecTimings` struct so the same widget covers room-temperature ratios + accelerated test ratios. Outputs `(code, valid, repeat, busy)`. NEC transmitter, RC5/RC6 receivers, automatic byte/inverse-byte validation, and tolerance windows all deferred to v2. Original entry follows: strictly carrier-modulated single-wire protocols rather than digital 1-wire, but the digital protocol on top of the 38 kHz / 36 kHz IR carrier is the interesting part. **NEC**: 9 ms leading burst + 4.5 ms space + 32 bits (address + ~address + command + ~command), each bit = 562.5 µs burst + variable space (1.69 ms = 1, 562.5 µs = 0). **RC5**: Manchester-encoded, 14 bits at 1.778 ms per half-bit. **RC6**: similar to RC5 with extensions and a longer leader. The receiver decodes pre-demodulated input from a 3-pin IR receiver module (TSOP4838, VS1838B); the transmitter modulates the carrier itself using (22) the shipped PWM widget at 38 kHz. Composes (1) edge detector, `core::counter` for pulse-width timing, (5) priority encoder for protocol auto-detection (the leading-burst pattern uniquely identifies which of the three protocols is in use), `fifo::synchronous` for buffering decoded commands, (22) PWM for the transmit-side carrier. References: NEC IR Remote Control Format (Renesas application note); Philips RC5 and RC6 specifications; Vishay TSOP4838 datasheet; LIRC (Linux Infrared Remote Control) protocol database. |
| 31 | ~~SENT receiver (SAE J2716)~~ (framing-helper v1) — shipped: `crates/rhdl-fpga/src/core/sent_rx.rs`. Detects sync pulses and emits per-nibble period measurements; **the host computes the nibble value** as `(period / tick_period) - 12`. 2-state FSM with 4-element `SentTimings` struct (nibble min/max, sync min/max, all in FPGA cycles). In-kernel nibble decode, CRC-4 (polynomial `0x1D`), tick-period auto-calibration, pause-pulse detection, and slow-channel decode all deferred to v2. Original entry follows: Single Edge Nibble Transmission, an automotive single-wire sensor interface used for absolute-position, pressure, and temperature sensors in modern engines and chassis systems. Asynchronous, no clock, no master-slave handshake — sensors transmit unidirectionally by pulse-width-modulating successive 4-bit nibbles. Each frame: synchronization pulse (56 ticks), status nibble, 6 data nibbles (24 bits of sensor data), CRC nibble, optional pause pulse. Tick period is sensor-defined (typically 3 µs but auto-calibrated from the sync pulse). Composes (1) edge detector for falling edges, `core::counter` for tick measurement, (16) CRC engine specialized to SENT's 4-bit polynomial, a tick-period auto-calibration unit using a moving average over the sync pulse. Niche — primarily automotive — but real and increasingly common in OEM stacks. Slots with (28) LIN and (25) CAN as the third leg of the automotive sensor/actuator interface set. References: SAE J2716 specification; Melexis MLX90324 application note; Allegro A1335 SENT mode datasheet; Infineon TLE5012B SENT mode application note. |
| 32 | ~~MDA / Hercules monochrome digital video output~~ — covered by `core::video_timing::VideoTimingCore` with MDA preset timings (`h_total=882, h_active=720, h_sync=746..844, v_total=370, v_active=350, v_sync=353..369`). Character ROM, attribute decoder, and framebuffer compose on top — deferred. Original entry follows: IBM Monochrome Display Adapter (720×350 @ 50 Hz, 16.257 MHz pixel clock, mono + intensity TTL outputs) and its Hercules graphics superset (720×348 graphics mode, 16 MHz pixel clock). Composes a horizontal counter and vertical counter for sync timing, sync-pulse generation with correct polarity (HSYNC negative, VSYNC negative for MDA), `core::ram` for a character/font ROM (typically 9×14 cells), an attribute decoder for the IBM monochrome attribute byte (normal / bright / underline / reverse / blink). Output is four TTL pins — HSYNC, VSYNC, video, intensity — directly drivable by FPGA I/O at 5 V or via a level shifter. ~150 LOC excluding the optional character ROM. The simplest of the video widgets and the natural starting point. Note: the IBM lineage strictly distinguishes MDA from EGA-in-mono-mode — this entry covers MDA / Hercules; the EGA-in-mono-mode variant (EGA timing with chrominance bits gated off) is a small parametric extension once the EGA timing core is in place. References: *IBM Personal Computer Hardware Reference Library — Monochrome Display and Printer Adapter* (1981); Hercules Computer Technology *Hercules Graphics Card* datasheet; *The PC Engineer's Reference Book* (Sanchez & Canton); *Programmer's Guide to the EGA, VGA, and Super VGA Cards* (Ferraro). |
| 33 | ~~CGA digital RGBI video output~~ (test-pattern v1, digital only) — shipped: `crates/rhdl-fpga/src/core/cga_rgbi.rs`. Wraps `core::video_timing::VideoTimingCore` with CGA's 320×200 @ 60 Hz timings; emits a 16-color RGBI test pattern (4-pixel-wide vertical bars cycling through the full palette) gated by the `active` region. `cga_320x200_60hz()` constructor for canonical IBM CGA timings. Framebuffer, character ROM + 80×25 text mode, attribute byte decode, composite-NTSC artifact-color path, configurable bar width, and pixel-clock divider all deferred to v2. Original entry follows: IBM Color Graphics Adapter, 4-bit RGBI digital output at 14.318 MHz pixel clock, 60 Hz NTSC-derived timing. Text modes 40×25 / 80×25, graphics modes 320×200 (4 colors) / 640×200 (mono), and the famous "mode-4-and-7" composite-artifact-color modes. Composes a horizontal/vertical counter pair, a 4-bit RGBI palette ROM, character/font ROM (8×8 cells), `core::ram` for the framebuffer, sync generation, and an *optional* composite-output path that ties together with #35 for the NTSC variant. Reuses much of #32's timing skeleton — both share the IBM CRTC-style horizontal/vertical counter pattern. ~250 LOC for digital-only; +composite path adds the complexity from #35. References: *IBM Color Graphics Adapter Technical Reference* (1981); *Programmer's Guide to the EGA, VGA, and Super VGA Cards* (Ferraro); 8088 MPH demo's exhaustive use of CGA composite artifact colors; the *Reenigne* CGA documentation series (Andrew Jenner's deep-dive into mode 4 + 7). |
| 34 | ~~VGA analog RGB video output~~ — timing core covered by `core::video_timing::VideoTimingCore` with VGA mode preset timings (640×480@60, 800×600@60, 1024×768@60). Framebuffer + DAC drive deferred. Original entry follows: the canonical "first video widget" for any FPGA learner. Parameterized over VESA DMT modes — 640×480@60 (25.175 MHz pixel clock), 800×600@60 (40 MHz), 1024×768@60 (65 MHz), 1280×1024@60 (108 MHz), 1920×1080@60 (148.5 MHz), and the rarer custom timings. HSYNC and VSYNC are TTL outputs with mode-specific polarity; R/G/B are analog (typically driven via a 3- to 8-bit-per-channel R-2R or weighted-resistor DAC built from FPGA pins, or a discrete ADV7123-class DAC chip for higher bit depth). Composes a horizontal counter, vertical counter, sync-pulse generator with correctly-timed front porch / sync / back porch / active region per VESA, `core::ram` for framebuffer or pattern generator, and a small palette LUT. The high-resolution modes have tight pixel-clock budgets (148.5 MHz for 1080p) — a natural test for the auto-pipelining work in `auto-pipelining-plan.md` once it ships. ~200 LOC for the timing-and-sync core, framebuffer is a separate composition. References every implementer needs: *VESA Display Monitor Timing Standard (DMT)* and *VESA Generalized Timing Formula (GTF)*; the original *IBM Personal System/2 and PC BIOS Interface Technical Reference* for legacy 640×480; FPGA4Fun's VGA tutorial; Mike Field's *FPGA VGA* examples; Tom Verbeure's blog on FPGA video. |
| 35 | ~~Composite NTSC video generator~~ (monochrome v1) — shipped: `crates/rhdl-fpga/src/core/ntsc_composite.rs`. Wraps `core::video_timing::VideoTimingCore` with NTSC timings; emits a 2-bit composite-video output that maps to the standard composite levels (00 = sync, 01 = blank, 10/11 = picture luma) for use with a 2-bit R-2R DAC. Simplified VSYNC (single broad pulse rather than the 9-line equalize/vsync/equalize sequence). Color encoder, standards-compliant VSYNC, 480i interlace, PAL variant, and pixel-clock divider all deferred to v2. `ntsc_240p()` constructor for canonical NTSC-progressive timings. Original entry follows: the hard one. Single-wire analog signal carrying luminance + chrominance + sync, with a 3.579545 MHz subcarrier (NTSC) or 4.43361875 MHz (PAL) on which color information is QAM-modulated. Frame structure: equalizing pulses, vertical sync, equalizing pulses, vertical blanking with color burst, then 240/288 active lines per field, two interlaced fields per frame. Color requires a numerically-controlled oscillator (NCO) at the subcarrier frequency, a QAM modulator with U/V components (NTSC) or U/V with line-alternating phase (PAL), and a colorburst gate during back porch. Output drives a fast 8-bit DAC (typical AD724 reference design), a discrete R-2R ladder, or — for cheap-and-cheerful demos — a high-rate PWM that the AC-coupled output filter integrates. Monochrome variant skips the subcarrier and is much simpler. Composes a system-clock-to-pixel-clock divider, the H/V counter pattern from #32–34, a sin/cos lookup table for the NCO, a small mixer/QAM stage, the 8-bit DAC drive logic. ~400–600 LOC for color, ~200 LOC for mono. Pure-RHDL implementation requires care — auto-pipelining (per `auto-pipelining-plan.md`) is highly relevant since the QAM mixer plus DAC drive creates a long combinational path. References: ITU-R BT.470 (composite color standards, NTSC and PAL); ITU-R BT.601 (component digital video baseline); *Video Demystified* (Keith Jack) — the canonical engineer's reference; Don Lancaster's *TV Typewriter Cookbook* (1976) — historical but still pedagogically useful for sync generation; the Atari 2600 TIA chip documentation as a minimal-implementation reference; FPGA4Fun PAL tutorial. |
| 36 | ~~PWM / sigma-delta stereo audio output~~ (naive PWM v1) — shipped: `crates/rhdl-fpga/src/core/audio_pwm.rs`. Two parallel `core::pwm::PwmGenerator` channels driven by a shared sample-rate divider; `sample_request` pulse to the host on each sample boundary. Sigma-delta noise-shaping deferred (host can wrap upstream). Original entry follows: encodes 16-bit (or higher) signed audio samples as a PWM duty cycle at a high carrier frequency (typically 384 kHz to 4 MHz), then external RC low-pass filter integrates to recover analog. First-order PWM is straightforward but introduces audible quantization above 5–6 effective bits at moderate carrier rates; a 1st- or 2nd-order sigma-delta modulator pushes the noise above the audio band and recovers near-CD quality from a 1-bit output. Two parallel channels for stereo. Composes (22) the shipped PWM widget for the carrier, `core::counter` for sample-clock generation (typically 48 kHz audio sample rate from the system clock via an integer divider), `fifo::synchronous` for streaming samples in (one FIFO per channel), an optional 1st/2nd-order sigma-delta modulator state machine. ~120 LOC for naive PWM, ~250 LOC with sigma-delta. Used everywhere from the Pi Pico to the Game Boy Advance to dozens of MCU audio examples. Reference designs: *Pi Pico SDK PWM audio example* (`pico-extras/audio/`); *Texas Instruments SLAA414* (sigma-delta DAC techniques); *Digilent Pmod AMP2* schematic and reference design; the *uzebox* console (Atmel-AVR-based, uses PWM video + PWM audio for a complete game system, a classic minimal-hardware reference). The widget naturally pairs with #32–35 above to assemble a complete "video + audio" demo system on bare FPGA hardware. |
| 37 | ~~MIDI TX / RX~~ (wire layer v1) — shipped: `crates/rhdl-fpga/src/core/midi.rs`. Composes `core::uart`. Tags incoming bytes as status/data and remembers the most recent status byte for running-status-aware downstream parsing. Full message-level FSM (Note On / SysEx / etc.) deferred — the byte stream this widget exposes is what that FSM would consume. Original entry follows: 31.25 kbit/s asynchronous serial running an 8-N-1 byte stream with a small message-level FSM on top. The *wire* protocol is just a UART; the *message* protocol is what makes MIDI a distinct widget. Status bytes (MSB = 1) introduce voice messages (Note On / Note Off / Aftertouch / Control Change / Program Change / Pitch Bend / Channel Pressure — 1–2 data bytes each) or system messages (SysEx variable-length terminated by 0xF7, MIDI Time Code, Song Position Pointer, Tune Request). System Real-Time messages (0xF8–0xFF: Clock, Start, Continue, Stop, Active Sensing, System Reset) may interleave anywhere in a multi-byte message and must be passed through without disturbing the running parser state. Running status omits repeated status bytes — the receiver remembers the last status seen. Composes (18) UART TX/RX with a fixed 31250-baud divider, a small status/data parser FSM (`Idle → CollectData → EmitMessage`), a running-status DFF tracker, `fifo::synchronous` for the variable-length SysEx buffer, and an enum-with-payload structured-message output type — `enum MidiMessage { NoteOn { ch, note, vel }, NoteOff { ch, note, vel }, Cc { ch, ctrl, val }, ProgramChange { ch, prog }, PitchBend { ch, val }, SysEx { id, payload }, RealTime(RtKind) }` — another textbook RHDL ADT-with-payload demo. The TX side serializes the same enum back into bytes and respects running-status optimization. Hardware note: the standard MIDI input uses a 6N138/6N139 optoisolator on a 5 mA current loop for DC isolation — outside the FPGA but worth documenting in the example schematic. References: *MIDI 1.0 Detailed Specification* (MIDI Manufacturers Association); MMA Recommended Practices RP-001 through RP-052; Roland *MPU-401 MIDI Processing Unit Reference*; Linux ALSA `sound/core/seq/seq_midi_event.c`; Arduino *MIDI Library* (Forty Seven Effects). |
| 38 | **MIPI DBI Type C display driver** (parameterized over controller-chip family) — 4-wire SPI plus `D/C#` (data/command) plus `RESET#` plus optional `BUSY` input. The single most useful display widget — covers the majority of small-color-TFT and color-OLED hobbyist projects. Same protocol layer drives Sitronix ST7735 / ST7789 / ST7796, Ilitek ILI9341 / ILI9488, Galaxycore GC9A01 (1.28″ round 240×240, the smartwatch-style display), Solomon SSD1351 / SSD1331 (color OLEDs), and SSD1306 / SH1106 (mono OLEDs in SPI mode). The SPI bit-level transport is identical across all of them — only the *command set* and pixel format differ, captured as per-controller init-sequence ROM tables. Composes (19) SPI master, an init-sequence ROM, a pixel-stream FSM that tags writes as data vs command via the `D/C#` pin, optional partial-refresh window commands, and `fifo::synchronous` for buffered pixel streaming. ~200 LOC plus per-controller init tables. References: Sitronix ST7789 V1.0 datasheet; Ilitek ILI9341 / ILI9488 datasheets; *Adafruit GFX Library* (open-source reference for the init sequences used in practice); MIPI Alliance *Display Bus Interface (DBI)* specification v2.0. |
| 39 | **HDMI / DVI TMDS transmitter** — pure-fabric video output. Three TMDS differential pairs (one per color channel) at 10× the pixel clock plus a clock pair. Each pixel is TMDS-encoded (a two-stage transform: 8 → 9-bit transition-minimized + 1 DC-balance bit = 10 bits), then shifted out 10:1 through DDR-clocked output pins. Reuses the H/V timing core from (34) VGA almost unchanged — at the timing layer DVI is identical to VGA, only the output stage differs. The DVI subset (video only, no audio) is the achievable Tier-3 entry; HDMI audio islands and HDCP encryption are Tier-4 territory. Composes (34)-style H/V counter + sync, a TMDS encoder (combinational 8 → 10 bit lookup plus state-tracked DC balance bit), and a 10:1 OSERDES (vendor primitive on Xilinx via OSERDESE2 / OSERDESE3; soft 10:1 SerDes on small FPGAs using DDR primitives). ~250 LOC for the encoder + serializer separate from the timing core. **Vendor-primitive architecture** (per `vendor-primitive-architecture.md`) is highly relevant for the SerDes — Xilinx OSERDESE2, Lattice ODDR, Efinix DDR primitive, etc. References: DVI 1.0 Specification (DDWG); HDMI 1.4 Specification (sections on TMDS); *fpga4fun* DVI on FPGA tutorial; Mike Field's *Hamsterworks* HDMI examples; Tom Verbeure's blog on FPGA HDMI. |
| 40 | **E-paper SPI driver (Solomon SSD16xx family)** — drives SSD1608 / SSD1619 / SSD1633 / SSD1675 / SSD1677 controllers, which between them cover most Waveshare and Good Display 1.54″ / 2.13″ / 2.9″ / 4.2″ / 7.5″ / 10.3″ e-paper modules. Standard signal set: SCK, MOSI, MISO (rare — many panels are write-only), CS#, D/C#, RESET#, BUSY# (driven low while the controller is refreshing). The unusual part is the *waveform table* — e-paper pixels are transitioned by sequences of multi-frame voltage pulses, with different tables for full-refresh, partial-refresh, and fast-grayscale modes. The host loads the framebuffer, selects a waveform mode, kicks off a refresh, and waits for BUSY# to drop (which can take 100 ms to several seconds for full refresh). Composes (19) SPI master with command/data tagging via D/C#, (1) edge detector for BUSY# de-assertion, `fifo::synchronous` for the pixel stream, and a per-controller waveform-mode parameter table. ~200 LOC excluding the waveform tables. References: Solomon SSD1608 / SSD1619 / SSD1633 datasheets; Waveshare e-paper wiki and reference Arduino libraries; Pervasive Displays *EPD Reference Design Guide*; *E Ink Corp* application notes on waveform tables. |
| 41 | **HD44780 character LCD driver** — the canonical 16×2 / 20×4 character LCD interface. 8-bit or 4-bit parallel mode (the 4-bit mode is what most projects use; saves four GPIOs). Pins: D[3:0] or D[7:0], RS (register select — 0 = command, 1 = data), RW (read/write — typically tied to 0 for write-only), E (enable strobe; latches on the falling edge). Operations are bounded by the controller's internal busy flag, but most implementations just wait fixed-duration times since the timing is in microseconds. Init sequence is well-defined (function set → display control → entry mode → clear). Compatible clones: Samsung KS0066, Sitronix ST7066, Sunlike SPLC780. Composes `core::counter` for the timing waits, a small command/data state machine, optional `fifo::synchronous` for queueing characters. ~150 LOC. Pedagogically valuable as the canonical "send-command-then-data" parallel-bus widget. References: Hitachi *HD44780U Dot Matrix Liquid Crystal Display Controller/Driver* datasheet; Samsung KS0066 datasheet; Sitronix ST7066 datasheet. |
| 42 | **MIPI DPI / RGB parallel display driver** — for large TFT panels (5″ / 7″ / 10″) without integrated controller. The host streams pixels in real time exactly like VGA, just at LVTTL voltages and with a different pin-out: VSYNC, HSYNC, DE (data enable, marks active region), DCLK, R[7:0], G[7:0], B[7:0]. Reuses the H/V timing core from (34) VGA almost wholesale — same counters, same sync generator, same active-region detection. The only differences are voltage levels and the addition of an explicit DE pin (which is just the active-region flag from the timing core). Composes (34) VGA timing core, plus a small output-stage that re-bundles the active-region pixel into the DPI pin format. ~80 LOC on top of the VGA core. Single biggest user: 800×480 5″ and 1024×600 7″ TFT panels common on FPGA dev boards (Numato, Digilent, Olimex, Terasic). References: MIPI Alliance *Display Pixel Interface (DPI)* specification; the *Digilent Pmod RGB-Display* reference design; *EastRising* and *Riverdi* TFT panel datasheets (typical 5″/7″ panel docs). |
| 43 | **MIPI DBI Type B (8080 parallel)** — same controller chips as (38) but using their parallel-bus interface mode for higher bandwidth. Pins: D[7:0] or D[15:0], CS#, WR#, RD#, D/C#, RESET#. Useful when SPI bandwidth is insufficient — e.g., 480×320×16-bit at 60 fps is ~150 Mbit/s, achievable over fast SPI but borderline; 8080-16 parallel at a 10 MHz cycle is comfortable. Naturally implemented as a parametric extension of (38) sharing the init-sequence ROM and pixel-stream FSM, with the bus-driver layer swapped from SPI to 8080-parallel. ~150 LOC standalone, ~50 LOC if parameterized over (38). References: same controller datasheets as (38) — they document both modes; Ilitek ILI9488 *MCU Parallel Interface* section; ST7789 *Type B parallel* timing diagrams. |
| 44 | **SMBus / Smart Battery System host** — extends (21) I2C master with the SMBus protocol layer: stricter timing (10 kHz minimum so slaves can detect bus hangs), PEC (Packet Error Check, CRC-8 with polynomial `0x07`), block read / block write, send-byte / receive-byte, and host-notify (slave-initiated alerts via `SMBALERT#`). Smart Battery System (SBS) sits on top of SMBus — a standardized command set per the SBS specification for reading battery voltage, current, temperature, charge-remaining, cycle count, manufacturer info. Used by laptop battery packs (TI bq40xxx series gauges) and many power-management ICs. Composes (21) I2C master, (16) CRC engine specialized to CRC-8 polynomial `0x07`, a small SBS command-set state machine, (17) register file for status export. ~250 LOC. References: SMBus 3.2 specification (System Management Interface Forum); Smart Battery Specification 1.1 (SBS Implementers Forum); TI bq40z80 *Technical Reference Manual* (SLUUA45); TI bq25895 datasheet. |
| 45 | **TI HDQ master (single-wire fuel-gauge protocol)** — Texas Instruments' proprietary single-wire interface for their bq274xx / bq278xx fuel-gauge ICs. Async master-driven: a "break" pulse starts a transaction; each bit is a fixed cycle period (typically 200 µs) where a short low pulse encodes "1" and a long low pulse encodes "0". Frame: address byte + read/write bit, then 16 bits of data. Slower and simpler than 1-Wire but conceptually similar. Composes `core::counter` for bit timing, (1) edge detector for slave-response pulses, `tristate::simple` for the bidirectional line, `fifo::synchronous` for a small command queue. ~120 LOC. Niche but real — every major TI fuel-gauge IC supports HDQ as its low-pin-count interface. References: TI *bq2750 / bq27425 / bq27542 HDQ and SDQ Communication Protocol* application note; TI Technical Reference Manual SLUUA45 §HDQ. |
| 46 | **Battery management / charger control state machine** — coordinates charge / discharge / protection. Composes (21) I2C master or (44) SMBus host (depending on the charger chip) for parameter setup and status polling, `core::ram` for charge-history logging, `core::counter` for charge-stage timeouts, (17) register file for status export to the rest of the system. State machine: `Disconnected → Detecting → PreCharge → ConstantCurrent → ConstantVoltage → Termination → Done`, with safety transitions to `Fault` on over-temperature / over-current / over-voltage / timeout. The actual charging algorithm runs in the dedicated charger IC (TI bq25xxx / bq24xxx, Maxim MAX17xxx, Microchip MCP73xxx) — this widget orchestrates *which* parameters to set and *when* to enable / disable charge, monitors progress, and exposes status. ~300 LOC. Strategic value: combined with (44) SMBus and (45) HDQ, this gives RHDL the full battery-management interface set, opening industrial / portable / IoT FPGA applications where running off a battery is a baseline expectation. References: TI bq25895 *Single-Cell 5-A Li-Ion Battery Charger* datasheet; TI bq24295 datasheet; Maxim MAX17211 fuel gauge with charger datasheet; Microchip MCP73831 datasheet (the simple-linear-charger reference — no comms, just enable / PGOOD / STAT pins); IEC 62133 (battery safety standard). |
| 47 | **SMPTE LTC (Linear Timecode) reader / writer** — SMPTE 12M-2, the audio-wire timecode standard used in broadcast video, film production, and live performance to synchronize devices. 80-bit frame structure: 64 timecode bits (hours/minutes/seconds/frames as 8 BCD nibbles plus user bits) + 16-bit sync word `0x3FFD`. Biphase-mark-encoded so the signal is self-clocking and polarity-insensitive — every bit boundary has a transition; mid-bit transition encodes 1, no mid-bit transition encodes 0. Bit rate scales with frame rate (typically 2400 bps for 30 fps, 1920 bps for 24 fps). Drop-frame variant for 29.97 fps NTSC compensates for the 0.1% rate offset. Composes `core::counter` for bit timing, (1) edge detector for biphase transitions, a small biphase-mark decoder/encoder FSM, `fifo::synchronous` for buffered timecode events, and a structured timecode output type — `struct LtcFrame { hours: b5, minutes: b6, seconds: b6, frames: b6, drop_frame: bool, color_frame: bool, user_bits: b32 }`. ~200 LOC. Pairs naturally with #63 SDI (which carries embedded LTC in its ancillary-data channel) and with #36 PWM stereo audio (LTC is often distributed on an analog audio wire). References: SMPTE ST 12-1:2014 / 12-2:2014 (Time and Control Code); SMPTE RP 188 (Transmission of Time Code in the Ancillary Data Space); the *Linear Timecode FAQ* by Brad Davis; the *libltc* C library (open-source LTC decoder/encoder, useful as a verification reference). |
| 48 | **RS485 master / multidrop** — TIA/EIA-485-A differential serial bus. Up to 32 nodes per segment by spec (often more with repeaters), 1200 m at 100 kbps or 12 m at 35 Mbps. Half-duplex by default — the bus has at most one transmitter active at a time, with explicit DE (driver enable) and RE (receiver enable) outputs that toggle a discrete RS485 transceiver IC (Maxim MAX485, TI SN65HVD7x, Linear LTC2862). After transmit, the driver must be disabled and the receiver enabled with a few-bit-time gap before the addressed slave responds. Composes (18) UART TX/RX wholesale (RS485 is just UART framing on a differential pair), `tristate::simple` at the pad if the FPGA drives the differential lines directly (rare — usually a transceiver IC handles that), `core::counter` for turnaround timing between transmit and receive phases, (1) edge detector for receive-line activity. ~120 LOC because (18) does the heavy lifting. Reference protocols carried on RS485: **Modbus RTU** (industrial automation, ~90% of RS485 deployments), **Profibus DP** (industrial fieldbus), **BACnet MS/TP** (building automation), **DMX512** (stage lighting), various MIDI hardware variants. Each of these is a small follow-up widget on top of the RS485 base. References: TIA/EIA-485-A; Maxim MAX485 datasheet; TI SN65HVD7x series datasheet; *Modbus over Serial Line Specification and Implementation Guide* V1.02 (Modbus IDA); Profibus DP V1 protocol specification; BACnet ASHRAE 135-2020 §9 (MS/TP); ANSI E1.11 (DMX512-A). |
| 49 | **DTMF (Dual-Tone Multi-Frequency) generator and detector** — the touch-tone dialing standard. Each digit (0–9, *, #, A–D) is encoded as a pair of simultaneous sine tones, one from a low-frequency group (697, 770, 852, 941 Hz) and one from a high-frequency group (1209, 1336, 1477, 1633 Hz). Generator: two NCOs (numerically-controlled oscillators) with a sin LUT, summed and scaled, fed to a DAC. Detector: classic Goertzel algorithm — efficient narrow-band tone detector at known frequencies, ~16 multiplies and adds per sample for all 8 frequencies, no FFT required. Composes `core::counter` for sample-clock timing, `core::ram` for the sin LUT, (15) MAC unit for the Goertzel filter computation, (36) PWM stereo audio for the analog output. ~250 LOC for both directions combined. Still useful today: IVR (interactive voice response) systems, telephone-controlled automation (the original "press 1 for sales"), accessibility devices, retro-computing demos. References: ITU-T Recommendation Q.23 (DTMF tone definitions); ITU-T Recommendation Q.24 (DTMF receiver requirements); G. Goertzel, *An Algorithm for the Evaluation of Finite Trigonometric Series*, American Mathematical Monthly 65(1), 1958; Texas Instruments SLAA189 *DTMF Decoder Implementation Using the MSP430* (algorithmic reference); Asterisk telephony software DTMF decoder (`main/dsp.c`, open-source reference). |
| 50 | **V.21 dialup modem** (300 bps full-duplex FSK) — the simplest meaningful dialup standard, the modem that started home computing's connection to the world. Two FSK channels share one phone line: originate uses 980 Hz (mark) / 1180 Hz (space); answer uses 1650 Hz (mark) / 1850 Hz (space). Full-duplex by frequency separation. Bit timing is asynchronous 8-N-1 framing on top of the FSK channel — the same UART framing as (18). Modulator: an NCO toggling between the two tone frequencies at the bit rate (300 bps), output through a sigma-delta or PWM DAC. Demodulator: two narrow-band Goertzel filters (one per FSK frequency), comparator on their magnitudes, then UART RX framing on the resulting bit stream. Composes `core::counter` for bit and sample timing, `core::ram` for the sin LUT, (15) MAC unit for the Goertzel computations, (18) UART RX/TX for framing, (36) PWM stereo audio for the analog output. ~500 LOC. **Out of scope** for this widget — the harder dialup-modem family: **V.22 / V.22bis** (1200/2400 bps, QAM with carrier recovery), **V.32 / V.32bis** (9600/14400 bps, trellis-coded QAM), **V.34** (33600 bps, adaptive line equalization), **V.90 / V.92** (56k via PCM digital path). Each requires substantial DSP — symbol-clock recovery, decision-feedback equalizers, trellis decoders — and would be a multi-month project on its own. V.21 stops at the achievable-and-fun point of the family while still being a real, historically-accurate dialup modem. References: ITU-T Recommendation V.21 (300 bps FSK); ITU-T Recommendations V.22 / V.22bis / V.32 / V.34 / V.90 (the harder follow-ups, listed for future reference); Bell 103 (the equivalent US standard, 300 bps FSK on slightly different frequencies); the *FSK demodulation* chapter of *Digital Communications: Fundamentals and Applications* (Sklar, 2001); soft-modem implementations in Asterisk and FreeSWITCH; the *spandsp* DSP library (open-source modem reference, MIT-licensed). |
| 51 | **MFM (Modified Frequency Modulation) encoder / decoder** — the self-clocking encoding scheme used by PC floppy drives (5.25″ and 3.5″) and older hard drives (ST-506 / ST-412). Each bit cell becomes 0 or 1 flux transitions on the read/write line: between two adjacent bits, write a clock transition only if both are 0; always write a data transition for a 1. This eliminates 90% of clock transitions versus straight FM encoding and doubles areal density while remaining self-clocking. Decoder side requires a software DPLL (digital phase-locked loop) to recover the clock from the transition stream — particularly tricky on real-world floppies where the bit-cell time can vary by ±10% with media wear, drive eccentricity, and ambient temperature. Encoder: ~50 LOC, a small shift-register-based FSM operating over a sliding 2-bit window. Decoder: ~200 LOC, a sequential FSM with hysteresis windows and rate-tracking. Composes (1) edge detector for transitions, `core::counter` for bit-cell window timing, (3) sync chain on the read-data input, optional `fifo::synchronous` for a small FIFO between the encoder and the write driver. Used by (52) floppy disk controller and historical hard-disk interfaces (ST-506 / ST-412 with Western Digital WD1010-class controllers, where MFM was the dominant rigid-disk encoding from ~1980–1989). References: Western Digital WD1772 datasheet (the floppy controller used in many 1980s home computers); Shugart Associates SA-400 Floppy Disk Drive Service Manual (the canonical 5.25″ floppy interface spec); Tony Diaz's *Floppy Disk Formats* compendium; the *Greaseweazle* project by Keir Fraser (open-source floppy controller used for archiving real floppies — excellent reference for real-world MFM timing tolerances and PLL behavior); the *FluxEngine* and *KryoFlux* projects (similar archival tools). |
| 52 | **Floppy disk controller** (NEC µPD765 / Intel 8272A compatible) — the classic FDC that did all the heavy lifting in PCs from 1981 onwards and in many home computers (Atari ST, Amiga, NeXT) before that. Implements the µPD765 command set: Read Data, Write Data, Read ID, Format Track, Recalibrate, Seek, Sense Interrupt Status, Specify (timing), Sense Drive Status. Each command is a 2–9 byte sequence with a result phase. Internally: command FSM, sector framing FSM (ID address mark + data address mark + 2-byte CRC per sector + gap bytes), step-pulse generator for head positioning, index-pulse detector for rotation reference, drive selection. Implements both single-density (FM, 250 kbps) and double-density (MFM, 500 kbps) modes — composes (51) MFM encoder/decoder for the latter. Drives 360 KB / 720 KB / 1.2 MB / 1.44 MB / 2.88 MB PC floppies plus the various non-IBM formats. Composes (51) MFM encoder/decoder, `core::counter` for step-pulse timing and index-pulse timeout, (1) edge detector for index pulses, (16) CRC engine specialized to CCITT-CRC16 with polynomial `0x1021` (the floppy CRC), (17) generic memory-mapped register file for the host-side command/status interface, `fifo::synchronous` for read/write data buffering. ~600 LOC. Pairs with the standard 34-pin Shugart-derived floppy interface. **Note:** Apple II / Macintosh GCR (Group-Coded Recording) is a different bit-cell scheme from MFM and would be a separate ~300 LOC widget on top of (51); not included in this entry. References: NEC µPD765A datasheet; Intel 8272A datasheet; *IBM PC Technical Reference* (1981) §A-XX (FDC programming); *Programming the PC Floppy Disk Controller* (Brian Pirie, classic 1990s technical reference); the Linux floppy driver `drivers/block/floppy.c`; FluxEngine / Greaseweazle / KryoFlux source code (modern open-source floppy archival projects). |
| 53 | **Parallel SCSI master / target** (SCSI-1 / SCSI-2, narrow 8-bit and wide 16-bit) — the classic Small Computer System Interface. 9 control signals (BSY, SEL, C/D, I/O, MSG, REQ, ACK, ATN, RST) plus the data lines plus parity. Asynchronous handshake (REQ/ACK) up to 5 MB/s for SCSI-1; synchronous handshake (Fast/Ultra) up to 320 MB/s for parallel SCSI. Bus-phase FSM: BUS FREE → ARBITRATION → SELECTION → COMMAND → DATA → STATUS → MESSAGE → BUS FREE. Each phase has its own request/response semantics. This widget implements the master (initiator) side, the target (peripheral) side, or both — symmetric protocol means the same FSM scaffolding covers both with role-specific arbitration and message-system handling. Composes `core::counter` for handshake and arbitration timing, (1) edge detector for REQ/ACK transitions, `core::dff` for the bus-phase state machine, (3) sync chain on the asynchronous control lines, `tristate::simple` for every bidirectional bus signal (SCSI is heavily multidirectional — every line is driven and sensed by multiple devices), (16) CRC engine for SCSI-3 EDC (Error Detection Code) when enabled. ~800 LOC for a complete initiator + target implementation — most of the complexity is in the bus-phase FSM and the message-system handling. Pre-Ultra speeds (5 MB/s asynchronous, 10 MB/s Fast SCSI) are achievable in pure fabric on any modern FPGA; Ultra2/Ultra160/Ultra320 require careful routing and may need vendor-specific I/O primitives, putting them at the edge of Tier 3 and into Tier 4 territory. For modern Serial Attached SCSI (SAS) — same command set, SERDES-based physical layer at 3/6/12/22.5 Gbps — that's a future Tier 4 §4b entry. References: ANSI X3.131-1986 (SCSI-1); ANSI X3.131-1994 (SCSI-2); ANSI INCITS 351:2001 (Ultra3 SCSI); Western Digital WD33C93A datasheet (a popular SCSI controller chip); Ralf Brown's *PC Interrupt List* §9 (SCSI BIOS details); the Linux SCSI subsystem (`drivers/scsi/`); the *SCSI Bench Reference* (Field & Ridge, 1991). |
| 54 | **NAND flash controller** (async ONFI 1.x parallel) — drives raw NAND flash chips at the chip-level interface. ONFI 1.x async mode is the legacy parallel interface: 8-bit or 16-bit data lines, plus CE# (chip enable), CLE (command latch enable), ALE (address latch enable), WE# (write enable), RE# (read enable), R/B# (ready / busy from the chip). Commands and addresses are multiplexed onto the data lines using the CLE/ALE strobes. Standard ONFI 1.0 mandatory command set: Reset (FFh), Read ID (90h), Read Status (70h), Page Read (00h, 30h), Page Program (80h, 10h), Block Erase (60h, D0h), Read Parameter Page (ECh). The widget handles command sequencing, page-level read/write with the column-then-row addressing scheme, R/B# polling between operations, and timing parameters per the chip's datasheet (tWP, tRP, tWHR, tWB are the critical ones). **Pairs with (55) BCH ECC** — raw NAND is unreliable; every page must be ECC-protected before write and verified on read. Composes `core::counter` for ONFI bus timings, (1) edge detector for R/B# transitions, (3) sync chain on R/B#, (17) generic register file for the controller's command-issue interface, `fifo::synchronous` for page-data buffering, optional `tristate::simple` on the data bus (most modern NAND uses a separate IO_t/IO_c arrangement that doesn't need tristate). ~500 LOC. Synchronous ONFI 2.0+ DDR mode and Toggle Mode NAND (Samsung/Toshiba) are out of scope for this widget — they need DDR I/O primitives and belong with the other vendor-primitive-dependent widgets. References: Open NAND Flash Interface (ONFI) Specification 1.0 — freely downloadable at onfi.org; Micron / Samsung / SK hynix / Kioxia (Toshiba) NAND datasheets (any modern part documents ONFI compliance in the first few pages); Linux MTD subsystem (`drivers/mtd/nand/`); U-Boot raw NAND drivers (`drivers/mtd/nand/raw/`); *NAND Flash Memory Technologies* (Aritome, IEEE Press 2015) for the device physics. |
| 55 | **BCH error-correcting-code engine** (encoder + decoder) — Bose–Chaudhuri–Hocquenghem code, the workhorse ECC for NAND flash and other unreliable storage. Parameterized over codeword length n, message length k, and correction capability t (number of bit errors correctable per codeword). Common configurations: BCH(4108, 4096, 1) for SLC NAND with 1-bit correction per 512-byte sector; BCH(8568, 8192, 32) or BCH(8688, 8192, 40) for modern MLC/TLC NAND. Encoder is polynomial-multiplier style — feed message bits through a shift register seeded with the generator polynomial; structurally similar to the (16) CRC engine but operating in GF(2^m) for larger m. Decoder is more involved: syndrome computation (n − k linear equations over GF(2^m)), Berlekamp–Massey or Peterson–Gorenstein–Zierler algorithm to find the error-locator polynomial, Chien search to find the error positions, then bit-flip correction. Composes (15) MAC unit for the Galois-field arithmetic in syndrome computation and Chien search, `core::ram` for storing syndromes and intermediate state, `fifo::synchronous` for data buffering, (16) CRC-pattern logic for the encoder. ~800 LOC for a modest BCH(t = 4) implementation, scaling roughly linearly with t. Pairs with (54) NAND flash controller; also useful for any storage protocol where t-bit error correction beyond simple CRC parity is required. References: S. Lin and D.J. Costello, *Error Control Coding* (2nd edition, Pearson 2004) — the canonical textbook; *BCH and Reed-Solomon Codes* (S.B. Wicker, Prentice Hall 1995); Linux NAND BCH driver `drivers/mtd/nand/raw/nand_bch.c`; the *libcorrect* C library (open-source ECC implementations, BSD-licensed); ECC-Tools project (open-source BCH/Reed-Solomon library). |
| 64 | **PS/2 keyboard controller** (host side) — drives the IBM PC-AT PS/2 keyboard interface, the synchronous serial protocol every PC keyboard from 1987 to ~2010 used (and that USB keyboards still emulate via legacy mode in the BIOS).  Two open-drain wires: `CLK` and `DATA`, each idling high through a 10 kΩ pull-up.  The keyboard sources the clock (10–16.7 kHz) and shifts an 11-bit frame on every burst: start (0) + 8 data LSB-first + odd parity + stop (1).  The host-side widget samples `DATA` on the falling edge of `CLK` (rising-edge oversampled with `cdc::synchronizer_chain` for metastability), assembles the 8-bit scan code, validates parity, and exposes `(scan_code, valid)`.  The host-to-keyboard direction (set LEDs, set typematic rate) is symmetric: the host pulls `CLK` low for ≥ 100 µs, releases, then drives `DATA` while the keyboard clocks.  v1: receive-only.  v2: bidirectional with the host-to-keyboard request-to-send dance.  Composes `cdc::synchronizer_chain` on `CLK` and `DATA`, `core::edge_detector` for the falling clock edge, `core::dff` for the 11-bit shift register and bit counter.  ~150 LOC v1.  References: IBM Personal System/2 Hardware Interface Technical Reference (1988); Adam Chapweske's PS/2 protocol reference (computer-engineering.org/ps2protocol/); the Linux i8042 driver `drivers/input/serio/i8042.c`; *The Indispensable PC Hardware Book* (Messmer, Addison-Wesley 1995). |
| 65 | **PS/2 mouse controller** (host side) — same wire protocol as (64), different framing.  PS/2 mouse packets are 3 bytes: button state + 9-bit signed X delta + 9-bit signed Y delta (sign bits in byte 0, magnitudes in bytes 1–2).  Microsoft IntelliMouse extension adds a 4th byte (Z scroll wheel + buttons 4/5).  Composes (64)'s 11-bit-frame receiver as a building block; adds a 3- or 4-byte packet assembler with framing-error recovery (a stuck slave eventually re-syncs on the per-byte stop bit).  v1: 3-byte standard packet.  v2: enable IntelliMouse mode via the standard host-to-mouse "Set Sample Rate to 200 / 100 / 80" magic sequence.  Composes (64) by re-using its byte-receiver stage plus a tiny 3-state packet-byte FSM (`Byte0 / Byte1 / Byte2 → emit packet → Byte0`).  ~100 LOC additional.  References: same as (64); the Microsoft *IntelliMouse PS/2 Driver* documentation for the wheel-extension magic sequence; the Linux mouse drivers `drivers/input/mouse/`. |
| 66 | **I²S (Inter-IC Sound) transmitter** — Philips' 1986 audio-bus standard, the canonical chip-to-chip digital-audio link used by every modern audio codec (CS43L22, ES9038, WM8731, AK4490, ESS9018, etc.).  Three wires: `BCLK` (bit clock), `LRCK` (left/right select, equals the audio sample rate), and `SD` (serial data, MSB-first, two's complement).  Standard Philips framing: data is shifted on the rising edge of `BCLK` *and* offset by one BCLK from the LRCK transition.  v1: master mode, transmit-only, 16-bit or 24-bit signed samples, configurable per-channel sample width (8/12/16/20/24/32).  Per-channel slot tracking via an N-bit shift register and a small FSM (`LeftSetup / LeftShift / RightSetup / RightShift → LeftSetup`).  v2: receive (RX) path; full-duplex transceiver; left-justified and right-justified framing variants.  TDM (8/16-channel) is a separate widget once anyone needs more than stereo.  Composes `core::dff` for the shift register and bit counter; emits BCLK / LRCK from a divided host clock.  ~200 LOC.  References: NXP / Philips *I²S Bus Specification* (1986, revised 1996); Cirrus Logic CS43L22 datasheet (the most heavily-cloned codec); Wolfson WM8731 datasheet (Raspberry Pi audio HAT favourite); Linux ALSA System on Chip (`sound/soc/`) drivers — every entry there speaks I²S to its codec. |
| 68 | **ECP / IEEE 1284 parallel port (Centronics)** — the IBM PC parallel port (DB-25 connector) used to drive every printer from the early 1980s through ~2010, and that survives today on industrial PCs and lab instrumentation (oscilloscopes, plotters, GPIB-to-parallel bridges).  The original *Centronics* mode is a simple output handshake: 8 data lines (`D0–D7`), `STROBE_n` (host → device, "data valid"), `ACK_n` (device → host, "data accepted"), `BUSY` (device → host, "I'm processing"), `PE` (paper-end), `SLCT` (selected/online), and the configuration outputs `AUTOFEED_n`, `INIT_n`, `SLCTIN_n`, `ERROR_n`.  IEEE 1284 (1994) added four bidirectional modes: *Nibble* (reverse-channel via the status lines), *Byte*, *EPP* (Enhanced Parallel Port — true bidirectional 2 MB/s), and *ECP* (Extended Capabilities Port — bidirectional + FIFO + RLE compression).  v1: Centronics output-only widget — host strobes `send` with each byte, the widget asserts `D[7:0]`, pulses `STROBE_n` low for `t_strobe` cycles, then waits for the device's `ACK_n` pulse.  Composes `core::dff` for the 8-bit data register and a small 4-state FSM (`Idle / Setup / StrobeLow / WaitAck → Idle`).  v2: IEEE 1284 negotiation handshake (the magic 1284-mode-select sequence) and *Nibble* reverse channel.  v3: EPP and ECP modes — these need a bidirectional data bus (wrap with `tristate::simple` at the pad), an FSM per protocol mode, and the ECP RLE encoder/decoder pipeline.  ~150 LOC v1.  References: IBM Personal Computer Technical Reference (1981, "Printer Adapter" chapter); IEEE Std 1284-1994 *Standard Signaling Method for a Bidirectional Parallel Peripheral Interface for Personal Computers* (and the 2000 revision); Centronics 101 printer documentation (the historical name); Linux `drivers/parport/` (the kernel's full IEEE-1284 implementation); Warp Nine Engineering's *IEEE 1284 Application Note* (the canonical bring-up reference).  This widget pairs naturally with the future ISA bus target (#67) since the parallel-port hardware was historically an ISA card. |
| 73 | **USB 1.1 (Low Speed + Full Speed) device controller** — Universal Serial Bus, the dominant peripheral interconnect since 1998.  USB 1.1 covers two speeds: **Low Speed** (1.5 Mbps, used by keyboards/mice/HID) and **Full Speed** (12 Mbps, used by audio/printers/storage).  Both are achievable in pure FPGA logic at modest board clocks (≥ 48 MHz for FS, ≥ 6 MHz for LS) — *no* vendor SerDes required, unlike USB 2.0 High Speed (480 Mbps, needs DDR I/O + a hardened ULPI PHY) and USB 3.x (5+ Gbps, needs full SerDes).  Wire layer: differential D+/D- with NRZI encoding and bit stuffing (insert a 0 after every 6 consecutive 1s).  Packet layer: token packets (IN/OUT/SETUP/SOF), data packets (DATA0/DATA1, with 16-bit CRC), handshake packets (ACK/NAK/STALL).  Transfer types: Control (mandatory for endpoint 0), Bulk, Interrupt, Isochronous.  v1 scope: **device-side Serial Interface Engine (SIE)** — wire-level NRZI/bit-stuffing en/decoder, packet PID parser, CRC5/CRC16 generator + checker, the device-side end of the token/data/handshake protocol, plus a single bidirectional control endpoint (EP0) that handles the standard USB requests (GET_DESCRIPTOR, SET_ADDRESS, SET_CONFIGURATION, etc.).  v2: bulk + interrupt endpoints; v3: USB classes (HID, CDC-ACM, MSC); v4: host-side controller (substantially larger scope — needs scheduler, transaction translator, etc.); v5: USB 2.0 HS via vendor ULPI-PHY interface.  Composes `core::dff` + `core::counter` for state, `core::crc` for the two USB CRC variants (5-bit on tokens, 16-bit on data), `cdc::synchronizer_chain` on D+/D-, `core::edge_detector` for SOP/EOP detection, `core::shift_register`-style logic for NRZI + bit stuffing, `tristate::simple` for the bidirectional D+/D- pair (both lines need driver-disable for the host's response packets).  ~1200 LOC for a working device-side SIE + EP0; full HID-class device (mouse/keyboard) on top is another ~300 LOC.  References: USB Implementers Forum *USB 1.1 Specification* (Sep 1998, free download via usb.org) and *USB 2.0 Specification* (Apr 2000, supersets 1.1); Jan Axelson, *USB Complete: The Developer's Guide* (5th ed, Lakeview 2015) — the canonical book; Linux kernel `drivers/usb/gadget/` for the Linux-side device API; the TinyFPGA-Bx USB bootloader and *Fomu* USB stack (open-source pure-FPGA USB 1.1 device implementations in Verilog/Migen, MIT licence) — these are the primary references for "USB on FPGA without a hardened PHY"; also the *VUSB* project (USB 1.1 device on an AVR microcontroller, GPL) for the PID/CRC/stuffing reference logic. |
| 71 | **IEEE 1394 FireWire (link layer / DMA)** — Apple's 1995 high-speed serial bus (FireWire 400 = S400 = 400 Mbps; FireWire 800 = S800 = 800 Mbps; later 1394b at 1.6 Gbps and 3.2 Gbps).  Three layers per IEEE 1394-2008 §1: PHY (handles arbitration, encoding, line states — typically a vendor-hardened block like the TI TSB81BA3 or LSI LS-K1394), Link (packetization, CRC, isochronous timing) — *this widget*, and Transaction (asynchronous reads/writes/locks, isochronous channels — also part of v1).  v1: link-layer + transaction-layer for async data + DC-balanced packet framing, talking to an external PHY via the standard PHY-Link Interface (PIL).  Supports 1394a / 1394b at S100 / S200 / S400 line rates (S800+ requires DDR I/O which depends on vendor primitives).  Composes `core::dff` for the link-state machine, `core::crc` for the CRC32 packet trailer, `core::round_robin_arbiter` for outbound queue arbitration, `cdc::synchronizer_chain` on the PHY-side handshake (the link runs at the bus base rate while the FPGA fabric runs at its own rate).  v2: isochronous timing (the 8 kHz cycle-master clock + per-channel slot allocation); v3: AV/C protocol (digital camera control); v4: SBP-2 (FireWire storage); v5: 1394b at S800.  ~1200 LOC for the link + transaction layers in v1.  References: IEEE Std 1394-2008 *High-Performance Serial Bus*; IEEE Std 1394a-2000 (S400 base spec); Texas Instruments TSB81BA3 PHY datasheet (the canonical PHY-Link Interface reference); Apple *Mac OS X FireWire System Reference* (covers the Apple-side host stack); Linux kernel `drivers/firewire/` (full-stack open-source implementation); the libdc1394 library (DV-camera-focused but exercises every layer); *FireWire System Architecture* (Anderson, MindShare 1999) — the canonical textbook. |
| 72 | **IEEE 1588 / PTP (Precision Time Protocol)** — IEEE 1588-2008 *Precision Time Protocol* — the high-precision (nanosecond-class) network time synchronization standard used in industrial automation, professional audio (AVB / TSN), broadcast (SMPTE 2059 ST), 5G fronthaul, financial trading, and scientific instrumentation.  Carried over Ethernet (Annex F, the most common, "Layer 2" or "PTPv2 Ethernet") or UDP (Annexes D/E).  Two modes: **One-Step** (single Sync message contains the timestamp at egress) and **Two-Step** (Sync followed by Follow-Up with the actual hardware-captured egress timestamp — most common because hardware timestamping is easier).  Message types: Sync (master→slave timing), Delay_Req / Delay_Resp (path-delay measurement), Pdelay_Req / Pdelay_Resp / Pdelay_Resp_Follow_Up (peer-delay variant), Announce (master selection / Best Master Clock Algorithm), Management, Signaling.  Hardware-side requirements: ingress + egress timestamp capture on every PTP packet (precision ≤ 8 ns for telco-grade, ≤ 50 ns for industrial-grade), local oscillator with a digitally-controlled rate (so the slave can servo its clock to the master's), and a PHY that exposes the SFD-detect strobe (the standard hook for capturing the timestamp at the canonical "ns timing reference plane").  v1 scope: hardware timestamp capture + Sync/Delay_Req message-type recognition + a slave-side servo loop that adjusts a local PI-controlled DPLL to track the master.  Composes `core::counter` for the local timestamp counter, `core::edge_detector` on the PHY's SFD strobe, `core::ram` for the timestamp queue, the future Ethernet MAC widget for L2 packet handling, and a small DSP block for the PI servo (proportional + integral) on the slave clock.  Master-side, BMCA, peer-delay, IEEE 802.1AS profile (gPTP, the AVB / TSN profile), and the management protocol are v2/v3.  ~800 LOC v1 for the slave-side timestamp + servo path; full master + slave + BMCA is ~1500 LOC.  References: IEEE Std 1588-2008 *Standard for a Precision Clock Synchronization Protocol for Networked Measurement and Control Systems* (and the 2019 revision, IEEE 1588-2019); IEEE Std 802.1AS-2020 (the gPTP / AVB profile); SMPTE ST 2059-1/2 (the broadcast profile); ITU-T G.8275.1 (telecom slave profile); Linux `linuxptp` project (the canonical open-source implementation by Richard Cochran); ptpd2 (older but still useful reference); *IEEE 1588-2008 Standard for Precision Time Protocol* by John Eidson — the spec author's textbook companion; Endace DAG cards' hardware timestamping implementation as a reference for ns-class precision.  Pairs with the future Ethernet MAC widget for L2 PTP, and with the DSP track for the servo controller. |
| 70 | **GPIB / IEEE 488.1-2003 controller / talker / listener** — Hewlett-Packard's 1965 instrumentation bus (originally HP-IB, standardised as IEEE 488 in 1975 and refined in IEEE 488.1-2003 / IEC 60625-1).  The universal interconnect for laboratory instruments — oscilloscopes, function generators, DMMs, spectrum analyzers, network analyzers — every Tektronix / Keysight / Agilent / Rohde & Schwarz / Stanford Research bench instrument shipped with a GPIB port for ~50 years (now mostly transitioned to USB-TMC / LXI but the installed base is enormous).  16 wires: 8 bidirectional data (`DIO1–DIO8`), 3 handshake (`DAV`, `NRFD`, `NDAC` — the canonical 3-wire interlocked handshake), 5 management (`ATN` attention, `IFC` interface clear, `SRQ` service request, `REN` remote enable, `EOI` end or identify).  Open-collector outputs (or Schottky equivalents) wired to passive pullups — every device on the bus AND every controller drives the same pins.  Architecture (per IEEE 488.1-2003 §3 + Annex B):  Each device implements a *State Machine Set* (SH source-handshake, AH acceptor-handshake, T talker, L listener, SR service-request, RL remote-local, PP parallel-poll, DC device-clear, DT device-trigger, C controller — 10 state machines total).  v1 scope: **single-controller mode** (only one C-machine on the bus), with full SH / AH / T / L / DC / DT / SR state machines per IEEE 488.1-2003 Tables 5–14.  PP and the multi-controller take-control / pass-control protocol are v2.  Composes `core::dff` for each of the 10 sub-state-machines (or a subset in v1), `cdc::synchronizer_chain` on every input control line (the bus is fully asynchronous to the FPGA clock), `core::edge_detector` on the handshake transitions, `tristate::simple` per `DIO` line and per management line (the bus is heavily multidirectional — `ATN` is driven by C, sampled by everyone; `SRQ` is driven by any device requesting service, sampled by C; etc.).  v1 ~600 LOC for a working controller + talker + listener; full IEEE 488.1-2003 conformance with all 10 state machines is ~1000 LOC.  References: IEEE Std 488.1-2003 (free download via IEEE GET program for select standards); IEEE Std 488.2-1992 (the message-encoding layer that sits on top of 488.1, also called "IEEE 488.2 Common Commands"); HP Application Note 1465 *GPIB Tutorial*; National Instruments NI-488.2 documentation (the reference open-source implementation in `linux-gpib` on SourceForge); Steve Curatolo's *GPIB-32 Programmer's Reference Manual*; Tektronix TM5000-series instrument programming manuals (every major Tek scope from the 1980s ships with a chapter on its 488 implementation).  Pairs naturally with the future SCPI parser (a Tier-4 widget that turns text command strings into bus operations). |
| 69 | **Modbus master / slave** (RTU + ASCII over RS-485, Modbus TCP) — Modicon's 1979 industrial-control protocol, the *single most-used* fieldbus in factory automation, building HVAC, water treatment, solar inverters, and pretty much every PLC ever shipped.  Wire variants: **Modbus RTU** (binary, 8N1 over RS-485 / RS-232 / TIA-485, the original and still ~80 % of installed base), **Modbus ASCII** (hex-text encoding of RTU frames, used on noisier links), **Modbus TCP** (encapsulated over TCP/IP, port 502).  Frame structure: address byte + function code + data + CRC-16 (Modbus polynomial `0xA001`, reversed).  Standard function codes: `0x01` Read Coils, `0x02` Read Discrete Inputs, `0x03` Read Holding Registers, `0x04` Read Input Registers, `0x05` Write Single Coil, `0x06` Write Single Register, `0x0F` Write Multiple Coils, `0x10` Write Multiple Registers, plus diagnostics (`0x08`) and exception responses (function-code OR'd with `0x80`).  v1: **Modbus RTU master**, fixed function codes `0x03` and `0x06` (read holding registers, write single register).  Composes the existing `core::uart` for the byte-level transport, `core::crc` for the Modbus CRC, an edge-detector for the inter-frame gap (≥ 3.5 character times of bus silence frames the start of a new RTU frame), `core::dff` + `core::counter` for the request/response state machine.  v2: extend to all standard function codes; *Modbus RTU slave* (symmetric receiver+responder); v3: **Modbus ASCII** (LRC checksum instead of CRC, hex-text encoding); v4: **Modbus TCP** — same PDU but no CRC and a 6-byte MBAP header; depends on a future Ethernet MAC widget.  ~250 LOC v1.  References: Modbus Organization *Modbus Application Protocol Specification V1.1b3* (free PDF at modbus.org); Modbus Organization *Modbus over Serial Line Specification V1.02*; Modicon's original 1979 *Modbus Protocol Reference Guide* (PI-MBUS-300); the `libmodbus` C library (LGPL, the canonical open-source implementation).  Pairs naturally with the existing `rs485_master` — Modbus RTU is the *most common* protocol carried over RS-485 in industrial settings. |
| 67 | **ISA-bus target** (8-bit XT mode + 16-bit AT mode) — the IBM PC's 1981 expansion bus, electrically alive on every motherboard until ~1999 and still very much present on industrial computers and CompactFlash-on-IDE retro-hardware.  20 address lines (XT) / 24 address lines (AT), 8 / 16 data lines, plus the control set: `IOR`, `IOW`, `MEMR`, `MEMW`, `AEN`, `BALE`, `BHE`, `IOCS16`, `MEMCS16`, `IOCHRDY`, `IOCHK`.  Plus the IRQ lines (IRQ3–7, 9–12, 14, 15) and the DMA channels (DRQ0–3, 5–7 + DACK).  v1: target-only widget that decodes a single I/O address range or single memory range and exposes `(read_strobe, write_strobe, addr_offset, data_in, data_out, ack)` to the user logic.  Bus-cycle FSM is small: BUS-IDLE → ADDR-VALID → CMD-LOW (`IOR`/`IOW`/`MEMR`/`MEMW` asserted) → DATA-DRIVE/SAMPLE → CMD-HIGH → BUS-IDLE.  IOCHRDY held low for wait-state insertion if the user side is slow.  v2: 16-bit AT data bus + IOCS16/MEMCS16 negotiation; v3: ISA bus master; v4: PCMCIA / 16-bit PC Card (electrically and structurally a refined ISA).  No DMA in v1 — the DMA controller (the Intel 8237A) is itself a widget and pairs with this one.  Composes `cdc::synchronizer_chain` on every input control signal (the FPGA clock isn't ISA's clock), `core::edge_detector` for command-strobe falling edges, `core::dff` for the bus-phase FSM and address latch, `tristate::simple` on the bidirectional data bus.  ~300 LOC v1.  References: IBM Technical Reference *Personal Computer XT* (1983) and *Personal Computer AT* (1984); the IEEE ISA-bus working group's *P996.1* specification (the only formal standard, posthumously); *Hardware Bible* (Winn Rosch, 6th edition 2003); Ralf Brown's *PC Interrupt List* §A (port maps).  Excellent reference implementation in the OPL2 / Soundblaster reverse-engineering communities (vogons.org). |

## Tier 4 — Larger systems

### 4a — Bus / interconnect / system widgets

| # | Widget | Notes |
|---|---|---|
| 56 | **Full AXI4** (read + write channels with bursting) | Reuses the existing `axi4lite::channel::{sender, receiver}` Ready/Valid handshake (already generic over `T: Digital`). New work is parameterized width types (`AxiData<D>`, `AxiAddr<A>`, `AxiStrobe<D>`), AW/W/B/AR/R beat structs with burst/length/size/ID fields, burst-aware controller and endpoint FSMs, outstanding-transactions ID-tracking in the switch. AXI4-Lite becomes the degenerate `Axi<32, 32, 0>` specialization. Required for DDR, PCIe, anything bandwidth-heavy. |
| 57 | **Wishbone classic + pipelined** | Alternative open bus. |
| 58 | **DMA engine** | Composes (12) round-robin arbiter, (17) register file, (56) AXI4. |
| 59 | **Ethernet MAC frontend** | The killer demo. Composes (16) CRC32, framing logic, CDC. |

### 4b — SERDES-based serial interfaces

These widgets all depend on hardened SERDES blocks (multi-gigabit transceivers / MGTs) on the target FPGA. They cannot be implemented in pure FPGA fabric — line rates of 1+ Gbps need silicon-level analog circuits and CDR (clock-data recovery) that fabric simply cannot do. Each widget is therefore the *combination* of a vendor-primitive wrapper (lane configuration, line rate, line coding) plus a pure-RHDL protocol-layer implementation on top.

**Hard dependency: `vendor-primitive-architecture.md` Phase 1+** — none of these widgets can ship until the `Target` trait exposes SERDES primitives. The relevant transceiver families per vendor: **Xilinx** GTH / GTX / GTY / GTM (UltraScale and 7-series); **Intel/Altera** GXB / GTH (Stratix, Arria); **Lattice** ECP5 SERDES quads, CertusPro-NX SERDES; **Microsemi/Microchip** PolarFire SERDES; **Efinix** Titanium SERDES. Each widget must work across at least the first three families through `Target` overrides; the protocol-layer code is identical, only the SERDES wrapping differs.

| # | Widget | Notes |
|---|---|---|
| 60 | **PCIe endpoint** (Gen1 ×1/×4 in pure-fabric where supported; Gen2/3/4 via hardened-block wrappers) | RHDL's role is the abstraction over the vendor-hardened PCIe blocks — which already implement the PHY (PIPE interface), data link layer, and on most modern parts the transaction layer too. RHDL supplies: configuration plumbing for the hardened block (lane count, link speed, BARs, requester ID, MSI/MSI-X), TLP (Transaction Layer Packet) generator/parser, and a bridge from PCIe TLPs to (56) Full AXI4 — so a PCIe endpoint exposes itself as an AXI4 master/slave that the rest of the design talks to. ~800 LOC for the TLP layer + AXI4 bridge, plus per-target wrapping of the hardened block. References: PCI Express 4.0 Base Specification (PCI-SIG); Xilinx UltraScale+ Integrated Block for PCI Express (PG213); Intel Stratix 10 H-Tile PCIe Hard IP User Guide (UG-S10-PCIE); Lattice CertusPro-NX PCIe Solution; Linux PCIe driver framework (`drivers/pci/`); LiteX `litepcie` (open-source pure-fabric PCIe Gen1/Gen2 implementation, useful as a fabric-only reference). |
| 61 | **JESD204B transmitter / receiver** (multi-lane, subclass 0 / 1 / 2 sync) | JEDEC standard for high-speed serial connections to data converters (ADCs / DACs). Up to L = 32 lanes at 312.5 Mbps to 12.5 Gbps per lane. 8b/10b line coding (handled by the SERDES primitive itself). Frame structure: octet → frame (F octets) → multiframe (K frames) → ILAS (Initial Lane Alignment Sequence) at link establishment. K-character control codes (K28.5 commas, R/A/Q markers) for synchronization. Subclass 0 has no SYSREF; subclass 1 uses a periodic SYSREF strobe for deterministic latency; subclass 2 uses SYNC~ handshake. Composes the SERDES primitive (vendor-specific wrapping), pure-RHDL frame/multiframe assembly, K-character insertion/detection, ILAS state machine for link-up, multi-lane skew correction (elastic FIFO per lane plus alignment to the multiframe boundary). Used heavily in software-defined radio (Xilinx RFSoC, Analog Devices AD9081/AD9082 transceivers), high-end ADCs/DACs, scientific instrumentation. ~600 LOC for the protocol layer per direction. References: JEDEC JESD204B (Serial Interface for Data Converters); Analog Devices Wiki on JESD204; Xilinx JESD204 LogiCORE IP (PG066); ADI HDL repository (open-source JESD204 implementations, GPL); Linux IIO driver framework (drivers/iio/). |
| 62 | **JESD204C** (follow-up to 204B; up to 32 Gbps per lane, 64b/66b coding) | Newer revision of the JEDEC standard. Backward-compatible at the application layer but uses 64b/66b line coding (vs. 8b/10b in 204B) for higher bandwidth efficiency, and supports lane rates up to 32 Gbps. Adds an explicit FEC option for the highest line rates. Reuses most of #61's frame/multiframe logic; the differences are line-coding (64b/66b scrambler/descrambler), the new sync mechanism (no SYSREF, instead embedded sync sequence), and the optional FEC. Natural follow-up entry once 204B ships. References: JEDEC JESD204C; ADI Wiki on JESD204C migration; Xilinx JESD204C IP. |
| 63 | **SDI video** (SD-SDI, HD-SDI, 3G-SDI, 6G-SDI, 12G-SDI; SMPTE 259M / 292M / 424M / 2081 / 2082) | Single-coax serial digital video used in broadcast, professional production, and live events. Carries 10-bit YCbCr 4:2:2 video at fixed line rates: 270 Mbps (SD-SDI), 1.485 Gbps (HD-SDI), 2.97 Gbps (3G-SDI), 5.94 Gbps (6G-SDI), 11.88 Gbps (12G-SDI). NRZI scrambling with the SMPTE-defined polynomial — distinct from 8b/10b — handled in fabric on top of the SERDES (most modern transceivers expose a "raw NRZ" mode for exactly this case). Frame structure per SMPTE 352M with embedded HANC (Horizontal Ancillary) and VANC (Vertical Ancillary) data spaces. Embedded audio per SMPTE 299M (HD-SDI) and embedded timecode per SMPTE RP 188 (which uses (47) LTC framing inside the ancillary-data channel — the two widgets compose). Composes the SERDES primitive (raw NRZ mode), SMPTE-polynomial scrambler/descrambler, frame generator with TRS (Timing Reference Signal — SAV/EAV) words, ANC packet inserter/extractor, and CRC checker (SMPTE RP 165 EDH). Pairs with #34 VGA / #42 DPI for converting between SDI and computer-style video. ~700 LOC for the protocol layer per direction (more for full ANC/audio support). References: SMPTE 259M (SD-SDI); SMPTE 292M (HD-SDI); SMPTE 424M (3G-SDI); SMPTE 2081-1 (6G-SDI); SMPTE 2082-1 (12G-SDI); SMPTE 352M (Payload Identification); SMPTE 299M (Embedded Audio); SMPTE RP 188 (LTC in ANC); SMPTE RP 165 (EDH error detection); Xilinx UltraScale+ SDI LogiCORE IP (PG071); Blackmagic Design *Decklink SDK Documentation*. |

---

## Recommended first eight (two-week scope)

In order. Each widget unblocks the next.

1. **Edge detector** — pick this first. ~20-line kernel. Perfect AI-workflow shakedown and the canonical reference implementation that other widget agents can pattern-match against.
2. **Pulse stretcher / one-shot** — same scale, complements (1).
3. **N-stage synchronizer chain** — generalizes `Sync1Bit`. Tiny.
4. **Priority encoder** — pure combinational, parameterized, foundation for (6) and (12).
5. **Decoder / one-hot ↔ binary** — pair with (4).
6. **Debouncer** — first widget that *composes* (1)+(2)+a counter. Demonstrates Tier-2 composition.
7. **Round-robin arbiter** — textbook design with rich test-coverage requirements; ideal LLM showcase.
8. **CRC engine** — last in this batch because it unblocks UART, Ethernet, SPI flash. Parameterizable CRC is ~150 LOC and has well-known reference values for testing.

After this batch, you have everything needed to write UART, SPI, debouncer-fronted GPIO, and a generic register-file peripheral with backpressure and arbitration. That set unlocks essentially every introductory FPGA tutorial — which is the leverage point for showcasing AI-assisted RHDL development.

## Why not start with UART

UART is what users *want*; what they *need* is the dependency stack underneath it. Starting with UART means building the edge detector, pulse stretcher, baud-rate divider, and shift register inline — none of them reusable, all entangled with UART-specific concerns. Two weeks later when someone wants SPI you rewrite the same pieces. Pay the depth-first cost up front; ship the protocols third.

## A concrete reusability dividend

If Tiers 0–2 are complete, the *combined* implementation cost of UART, SPI master, I2C master, and PWM is roughly the cost of UART alone done from scratch. That's the lever.

---

## Parallel work streams

The widget library is one of several independent tracks. The others unblock language-level, compiler-level, and verification capabilities that reshape how widgets get written:

- **`auto-pipelining-plan.md`** — design plan for letting the RHDL compiler automatically insert pipeline registers to meet a target clock frequency. Phase 1 covers pure combinational kernels; Phase 2 stateful kernels with hazard analysis; Phase 3 loop pipelining with II analysis. Lives at NTL level, after the Stage-3 optimization passes. Once shipped, widgets with long combinational paths (integer divider, MAC unit, wide CRC, AXI4 burst logic) become substantially easier to express because the timing-closure work moves from the user's source into the compiler.

- **`kernel-language-extensions.md`** — design spec for expanding the subset of Rust accepted inside `#[kernel]`. Phase 1 (pattern desugarings: `let-else`, or-patterns, range patterns, match guards, `@` bindings, array destructuring, `for x in array`, compile-time `assert`) unblocks readable state machines for UART/SPI/I2C. Phase 2 (`Bits<N>` method library: `count_ones`, `leading_zeros`, `reverse_bits`, saturating arithmetic) is a direct dependency of several roadmap widgets — popcount for ECC, leading-zero count for floating-point normalization, reverse-bits for serial protocols and CRC reflect. Phase 3 (`?` on `Option`/`Result`) and Phase 4 (custom traits, const-generic arithmetic) follow.

- **`vendor-primitive-architecture.md`** — design for the `Target` trait system that lets a single widget emit vendor-specific primitives (Xilinx DSP slices, Lattice EBR, Efinix multipliers, etc.) on targets that have them and fall back to portable Verilog elsewhere. Required for #39 HDMI/DVI (vendor SerDes) and unlocks optimal implementations for the MAC unit, BRAM widgets, PLL/MMCM wrappers, and several others. Widgets stay target-agnostic in their type signatures; the target is supplied at codegen via `Descriptor::hdl_for(&target)`.

- **`fsm-architecture.md`** — design for first-class FSM support: `#[derive(Fsm)]` ergonomic macro (Phase 1), static reachability and dead-state analysis (Phase 2), auto-generated state diagrams in rustdoc (Phase 3), formal verification via SymbiYosys-driven `#[fsm_invariant]` / `#[fsm_liveness]` / `#[fsm_cover]` properties (Phase 4), and an aspirational built-in BMC (Phase 5). FSM-shaped widgets are a large fraction of the roadmap (every protocol PHY, every arbiter, every register-mapped peripheral); this track is the force multiplier for building and verifying them.

The roadmap and these four design plans are best worked in parallel, not in sequence. A widget agent picking up an item from this list should consult the relevant companion documents before deciding how to express the widget — many widgets that are hard today are easy with one or two language extensions or an FSM derive in place, and noting that explicitly in a widget's design lets the work be re-prioritized cleanly.

---

## Updating this document

When a widget ships and is merged with full contract compliance (per `CLAUDE.md`), strike its row from the table by changing `**Edge detector**` to `~~Edge detector~~` and add a brief note pointing at the source path. New widgets discovered along the way are added at the appropriate tier; if you're not sure which tier, default to "one tier higher than the deepest existing dependency."

---

## Follow-ups (deferred from the first eight)

Recorded honestly per CLAUDE.md §15 — these widgets shipped with workarounds that should be tightened up when the underlying gaps are closed.

- **Async testbench cycle-alignment** — `cdc::synchronizer_chain::BitSyncChain` Tier-4 test uses `.skip(!0)`, which disables iverilog per-sample comparison. The same workaround already exists in `cdc::synchronizer::Sync1Bit`. Root cause: the asynchronous testbench framework (`rhdl-core/src/sim/testbench/asynchronous.rs`) compares DUT output against the Rust simulator at every event in the merged input stream, which doesn't align cycle-for-cycle with iverilog's `always @(posedge clock)` semantics for hand-written multi-domain widgets. Functional correctness is currently covered by Rust glitch_check + VCD digest. A fix would either (a) teach the testbench to sample only at clock edges of the destination domain, or (b) add a per-domain "settle" hook so Rust and iverilog agree on the observation moment. Affects all hand-written async widgets.

- **Verilog-snapshot length proxies** — `core::debouncer`, `core::round_robin_arbiter`, and `core::crc` `test_vlog_generation` tests check the *length* of the emitted Verilog rather than the full text. Length is a cheap regression canary but won't catch semantic-preserving changes. Replace with full `expect_test` snapshots once the codegen output stabilizes (the snapshots will be ~2-7 KB each and need re-blessing whenever any compiler pass changes).

- **Non-zero DFF reset value vs iverilog `initial`** — `core::crc::CrcEngine` Tier-4 test uses `.skip(2)` to bypass a one-cycle mismatch on the very first sample: the DFF resets to `0xFFFF`, which Verilog's `initial begin` block applies at time 0 but the Rust simulator only applies after the first rising edge (initial state is `dont_care`). After the first clock edge they agree. A clean fix would have the Rust simulator's `init()` for DFF use the configured reset value rather than `dont_care` — this is a one-line change to `core/dff.rs::Synchronous::init` but will affect every widget that uses a non-default DFF reset.

- **Pre-existing failures noted but not fixed** — `faulty_reducer::test_no_combinatorial_paths` was failing before any of the first-eight work; `cargo clippy --all -- -D warnings` produces ~34 errors in `rhdl-core` from a newer clippy version (mostly `collapsible_if`). Both are out of scope for the widget builds and are flagged here so they aren't lost.

- **vlog pretty-printer drops `;` after `wire [0:0] src_send;`** (and presumably other identifiers — exact trigger TBD). Discovered while building `cdc::slow_crosser`; worked around by renaming the wire to `send_in`. Reproducible: revert the rename and the generated Verilog from `slow_crosser::SlowCrosser::hdl()` drops the trailing semicolon, breaking iverilog. Investigation should look at `rhdl-vlog`'s parser/pretty-printer for any name-based special-casing.

- **`if/else`-evaluates-both-branches discoverability** — captured in CLAUDE.md §4 "Subtle semantics to internalize" so future agents don't have to rediscover it. Cross-validation via `test_kernel_vm_and_verilog_synchronous` is the test convention that exposes this class of bug; widgets that use variable shifts should always include such a test.

- **`as_bits()` defaults to outer kernel's const generic when called inside a kernel.** Hit while building `core::dht22` (40-bit frame intended to mask into Bits<16> halves) and again while building `core::can_master` (position arithmetic in Bits<11>/Bits<4>/Bits<15>). The compiler error is `cannot subtract Bits<X> from Bits<DIV_W>` — confusing the first time. Two workarounds today: (a) restructure to never need width conversion by computing in source width and using the generic `Shr<Bits<M>> for Bits<N>` impl (preferred — used in `can_master`); (b) expose the wider value as the API and let the host mask (used in `dht22`). A real fix would either teach kernel inference to look at the local `let x: Bits<N>` annotation (it doesn't currently) or warn when `as_bits()`'s width parameter has to be defaulted from the surrounding generic context.

- **`Synchronous` derive 12-tuple ceiling.** A widget with 13+ sub-circuit fields fails with `cannot compare (..., ..., ..., ()) with itself — PartialEq isn't derived for tuples beyond 12 elements`. Hit while building `core::can_master` (13 fields naturally; consolidated 3 stuff fields into a `StuffState` substruct to land at 11). Workaround is the substruct trick. The proper fix is to have the `Synchronous` macro emit a real generated struct with `#[derive(PartialEq)]` instead of a raw tuple, which would also improve compile-time error messages dramatically. Worth doing before the next 12+-field widget shows up (the 16550A UART will likely hit it).

- **CAN master v2 work** — bit-exact CRC validation against a software CAN model, CAN receiver (`CanReceiver`), 29-bit extended ID (CAN 2.0B), ACK slot detection / `ack_error` flag, programmable bit timing per ISO 11898-1 (Sync / Prop / Phase1 / Phase2), error-counter / bus-off state machine, SJA1000-compatible memory-mapped register interface. Each is a self-contained widget or extension; tracked in the `CHANGELOG.md` 2026-04-28 CAN entry.
